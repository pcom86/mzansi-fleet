import React from 'react';
import { View } from 'react-native';

// Placeholder components for web platform
export default View;

export const Marker = View;
export const Polyline = View;
export const PROVIDER_GOOGLE = null;
